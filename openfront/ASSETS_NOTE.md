# Excluded upstream assets
resources/maps/ (~374MB) and map-generator/ (~124MB) are excluded (pristine
upstream, regenerable). Restore from https://github.com/openfrontio/OpenFrontIO
before building. All mod source + the offline-APK fix are committed here.
