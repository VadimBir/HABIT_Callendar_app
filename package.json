{
  "name": "habit-calendar",
  "version": "1.0.0",
  "private": true,
  "description": "HABIT Calendar - adaptive calendar and task manager",
  "scripts": {
    "prepare:web": "node scripts/copy-web.js",
    "sync": "npm run prepare:web && cap sync android",
    "build:apk": "npm run prepare:web && cap sync android && cd android && ./gradlew assembleDebug"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  }
}
